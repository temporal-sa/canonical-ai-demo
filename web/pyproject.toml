[project]
name = "travel-agent-gateway"
version = "0.1.0"
description = "HTTP gateway + web UI for the durable travel-planner agent"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.14",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "pydantic>=2.9",
]

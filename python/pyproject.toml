[project]
name = "travel-agent"
version = "0.1.0"
description = "Durable agentic travel-planner ReAct loop on Temporal — Python"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.14",
    "anthropic>=0.69",
    "openai>=1.60",
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "psycopg[binary]>=3.2",
    "pydantic>=2.9",
    "python-dotenv>=1.0",
]
